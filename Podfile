# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FoodyCook​Book' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FoodyCook​Book
  pod 'Alamofire', '= 5.0' #networking framework
  pod 'Kingfisher', '~> 6.0'
  pod 'SVProgressHUD', '~> 2.2.5'

  target 'FoodyCook​BookTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FoodyCook​BookUITests' do
    # Pods for testing
  end

end
